// The Process module in Node.js is a built-in module that provides a way to interact with the current Node.js process. 

// console.log(process.argv);// run using "node process.js 2 4 mkxln"

// console.log(process.arch);

/* process.chdir('../socket');// change dir to socket
console.log(process.cwd()); */

// console.log(process.config);

/* console.log(process.cpuUsage());
let prev = process.cpuUsage();
console.log(process.cpuUsage(prev)); */

/* // A debugging port is a port that is used to enable remote debugging of an application or program.
// It allows developers to attach a debugger to the application and debug it remotely, without having to run the application on their local machine.
console.log(process.debugPort); */

// console.log(process.env);